package ch.adesso.examples.transactional;

/*
import javax.transaction.Transactional;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import ch.born.tecnet.core.domain.ordering.Task;
import ch.born.tecnet.core.domain.ordering.TaskRepository;

@Service
*/
public class TaskDemo {

	/*
    @Autowired
    private TaskRepository taskRepository;
    
    public void save() {
        Task task = new Task();
        task.setName("TEST");
        task.setExpectedHours(1.0F);
        task.setMinimumHours(1.0F);
        taskRepository.save(task);
    }

    public void saveThrowsNullPointerException() {
        Task task = new Task();
        task.setName("TEST");
        task.setExpectedHours(1.0F);
        task.setMinimumHours(1.0F);
        taskRepository.save(task);
        throw new NullPointerException();
    }

    public void saveThrowsException() throws Exception {
        Task task = new Task();
        task.setName("TEST");
        task.setExpectedHours(1.0F);
        task.setMinimumHours(1.0F);
        taskRepository.save(task);
        throw new Exception();
    }

    @Transactional
    public void saveTransactional() {
        Task task = new Task();
        task.setName("TEST");
        task.setExpectedHours(1.0F);
        task.setMinimumHours(1.0F);
        taskRepository.save(task);
    }

    @Transactional
    public void saveTransactionalThrowsNullPointerException() {
        Task task = new Task();
        task.setName("TEST");
        task.setExpectedHours(1.0F);
        task.setMinimumHours(1.0F);
        taskRepository.save(task);
        throw new NullPointerException();
    }

    @Transactional
    public void saveTransactionalThrowsException() throws Exception {
        Task task = new Task();
        task.setName("TEST");
        task.setExpectedHours(1.0F);
        task.setMinimumHours(1.0F);
        taskRepository.save(task);
        throw new Exception();
    }

    public void callSave() {
        saveTransactionalThrowsNullPointerException();
    }
    */

}
